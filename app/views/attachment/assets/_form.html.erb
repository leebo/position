<%= form_for(@attachment_asset, :html => {:multipart => true}) do |f| %>
  <% if @attachment_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attachment_asset.errors.count, "error") %> prohibited this admin_home from being saved:</h2>

      <ul>
      <% @attachment_asset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :attachment %>
      <br/>
      <%= f.file_field :attachment %>
      <!--<input type="file" name="attachment" id="my_test_room_attachment">-->
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
